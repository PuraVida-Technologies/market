import { TestingModule } from '@nestjs/testing';

describe('User resolvers (e2e)', () => {

  describe('Test', () => {
    it('must get true', async () => {
     expect(2+1).toBe(3)
    });
  });

 
});
